# OS / editor cruft
.DS_Store
._*
*~
*.swp
*.swo
.idea/
.vscode/

# Build output — everything generated lands in build/ (via .latexmkrc out_dir)
build/
.docker-image-stamp

# Stray LaTeX / biber artifacts, in case anything is run in the repo root
# instead of through latexmk's build/ dir. (The canonical Pruefungsamt.*.ps
# and pics/*.eps are tracked and unaffected — no *.ps / *.eps rules here.)
*.aux
*.bbl
*.bcf
*.blg
*.dvi
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.synctex.gz
*.toc
*.xdv
*-eps-converted-to.pdf
*.pdf

# Bibliography tooling
betterbib_cache.sqlite
